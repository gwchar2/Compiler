/* print fibonacci sequence */
a,b,c : int;
{
    a = 0;
    b = 1;

    while (a <= 1000) {
        while (b <= 0){
            c = a - b;

            while ( c != b){
                b = a + c;
            }
        }
       output(a);
       c = a + b;
       a = b;
       b = c;
    }
	
}
